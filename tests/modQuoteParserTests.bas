Attribute VB_Name="modQuoteParserTests"
Option Explicit
'Example:
'Dim o As Object
'Set o = JsonConverter.ParseJson(jsonText)
'Dim q As clsQuote
'Set q = ParseQuote(o)
Debug.Print "Parser test scaffold"
